[build]
  publish = "netlify-deploy/dist"
  command = "cd netlify-deploy && npm install && npm run build"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"